tags:
  - Auth
summary: Reset password
operationId: resetPassword
description: Reset the user's password using a token from email
security: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/auth/resetPassword.yaml'
responses:
  '200':
    $ref: '../../components/responses/auth/200_resetPwd.yaml'
  '404':
    $ref: '../../components/responses/auth/404.yaml'
